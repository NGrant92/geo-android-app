<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context="app.geo.activities.Camera" >
  
  <ImageView
      android:id="@+id/cacheImage"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_above="@+id/takePhoto"
      android:layout_alignParentRight="true"
      android:layout_alignParentTop="true"
      android:src="@drawable/camera"/>
  
  <Button
      android:id="@+id/takePhoto"
      android:layout_width="100dp"
      android:layout_height="wrap_content"
      android:layout_alignLeft="@+id/cacheImage"
      android:layout_alignParentBottom="true"
      android:background="@drawable/green_button"
      android:text="@string/take_photo"
      android:textColor="@color/colorWhite"/>
  
  <Button
      android:id="@+id/savePhoto"
      android:layout_width="100dp"
      android:layout_height="wrap_content"
      android:layout_alignBottom="@+id/takePhoto"
      android:layout_alignRight="@+id/cacheImage"
      android:layout_alignTop="@+id/takePhoto"
      android:text="@string/save_photo"
      android:textColor="@color/colorWhite"
      android:background="@drawable/green_button" />

</RelativeLayout>